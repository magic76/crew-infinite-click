# No Find It v6

`FLASHLIGHT_HUNT / FIND IT` is retired.

Changes:
- Removed signature scripts from `index.html`.
- ExperienceRuntime no longer schedules or starts signature moments.
- Stale/model `signatureMoment` values are forced to `NONE`.
- Gemini schema exposes only `NONE`.
- Gemini prompt explicitly avoids modal mini-games/search overlays.
- Legacy signature files are inert compatibility tombstones and are not loaded.
- FOMO v5, click juice, worlds, pooled effects, and sparse voice behavior are unchanged.
