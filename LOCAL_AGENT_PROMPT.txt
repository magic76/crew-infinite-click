Apply AI Clicker 0.36 on top of the current 0.35 project.

Read AGENT_APPLY.md first and follow it exactly.

This version is deliberately NOT a broad refactor. The only product goal is to make FLASHLIGHT_HUNT feel obviously different on a physical Android phone and to make HOLD / RELEASE / IDLE / WAIT meaningful player inputs.

Requirements:
- integrate SignatureMomentRuntime into the existing Pixi/game host
- reuse the existing Gemini Live session for signature banter
- do not create a second agent loop/session
- while a signature moment is active, suppress ordinary primitives/VFX/UI actions
- make pointer capture work on Android WebView
- keep safe-area bounds for target relocation
- run runFlashlightHuntDemo(signatureMoments) on-device
- do not declare success unless the screen is genuinely dark with a moving radial flashlight hole, the target escapes, HOLD is required, early release is detected, and idle behavior is visible
- run all JS tests and ./gradlew assembleDebug

Return:
1. modified files
2. test output
3. APK path
4. any integration issue found on the physical-device path
