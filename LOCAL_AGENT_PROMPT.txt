Apply ai-clicker 0.37 on top of the current 0.36 source.

Do not add new generic particle presets.

Primary requirements:
1. Make the player-facing game English-only.
2. Integrate GeminiConversationPolicy so Gemini Live voice is performed and high-contrast, not flat assistant narration.
3. Add SignatureMomentDirector to NORMAL PLAY, not only dev demos.
4. Guarantee SCREEN_SHATTER around the fourth meaningful player interaction.
5. SCREEN_SHATTER must capture the real Pixi/game canvas and animate 16 real image fragments over black.
6. During its wait phase, touching resets the timer; doing nothing restores the scene.
7. Pass delivery cues to signature BANTER requests using the existing Gemini Live session only.
8. Keep FLASHLIGHT_HUNT and make it English-only with the darker spotlight implementation.
9. Ensure signature moments temporarily own input and suppress ordinary VFX/actions.
10. Run every JS test and ./gradlew assembleDebug.

Do not report success merely because the code compiles. On a physical device, run both:
runScreenShatterDemo(signatureMoments)
runFlashlightHuntDemo(signatureMoments)

Then confirm the automatic cadence also triggers SCREEN_SHATTER during normal play within the first few meaningful interactions.
